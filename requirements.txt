# PromptGuard-Sched — pinned dependencies
#
# Exact pins (==), not ranges. This is a research system whose results need
# to be reproducible months from now — a floating range that silently
# upgrades torch/transformers between runs invalidates the ASR/CFD numbers.
# Bump deliberately, in its own commit, re-run the baseline gate after.

# --- Core runtime -----------------------------------------------------------
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1

# --- LLM backend -------------------------------------------------------------
# CUDA 12.1 build; RTX 3050 6GB dev target requires 4-bit quantization.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
transformers==4.46.2
accelerate==1.1.1
bitsandbytes==0.44.1
sentencepiece==0.2.0
huggingface-hub==0.26.2

# --- ILP refinement ------------------------------------------------------
pulp==2.9.0
# ortools==9.11.4210  # uncomment if you switch from PuLP/CBC to OR-Tools

# --- Data / simulator ------------------------------------------------------
numpy==2.1.3
pandas==2.2.3
networkx==3.4.2          # job/task DAG representation

# --- Persistence -------------------------------------------------------------
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10  # Postgres driver; SQLite needs no driver
redis==5.2.0              # optional LLM-response cache, keyed on (prompt_hash, model_id, temperature)

# --- API surface (§8 contracts) -----------------------------------------
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Statistics (§12 experimental matrix) ---------------------------------
scipy==1.14.1
statsmodels==0.14.4

# --- Testing -----------------------------------------------------------------
pytest==8.3.3
pytest-cov==6.0.0
pytest-asyncio==0.24.0
httpx==0.27.2             # FastAPI TestClient transport
locust==2.32.3            # load/latency testing (§15)

# --- Lint / type-check ---------------------------------------------------
ruff==0.7.4
mypy==1.13.0